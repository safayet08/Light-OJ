#include<bits/stdc++.h>
int main ()
{
    int T;
    scanf("%d",&T);
    for(int y=1; y<=T; y+=1)
    {
        int a,b;
        scanf("%d %d",&a,&b);
        printf("Case %d: %d\n",y,a+b);
    }
    return 0;
}
